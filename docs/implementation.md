# Implementation Log — Rain Dodger

Living document that tracks the app from Xcode template to a working app, and records the **user's changes in each phase**. Each phase is executed with `@implement <Phase>` and verified with `@review`; every change made in a phase is documented here (see **Change Log**). A phase is **done** only when everything in its checklist is complete, the project builds (`xcodebuild build -project RainDodger.xcodeproj -scheme RainDodger`), and `@review` reports clean.

## How to work this file

- Each phase is executed with `@implement <Phase>` (or ran as its own plan), then checked with `@review`.
- Tick checkboxes as work completes; move a phase to `Done` only when verified.
- Feature work always follows `.opencode/rules/003-project-guideline.md`; visual work follows the feature's design doc in `docs/designs/`; scope follows `docs/template/spec-guide.md`.
- Never commit or push here — the user does it explicitly with `@push`.

## Phase 0 — Baseline (current state)

- [x] Xcode project `RainDodger.xcodeproj` builds on iPhone 16 simulator
- [x] SwiftData `ModelContainer` wired in `RainDodgerApp.swift`
- [x] Docs in place: `docs/template/spec-guide.md`, `docs/template/design.md`, `.opencode/rules/003-project-guideline.md`
- [ ] Template `Item` model replaced by domain models (see Phase 2)

## Phase 1 — Foundation

Goal: repo-level skeleton every feature builds on. No visible features yet.

- [x] Feature folders created: `Models/`, `Services/`, `ViewModels/`, `Views/`
- [x] Service protocols with `async/await`: `LocationService`, `DestinationSearchService` (`WeatherService`/`DirectionsService` deferred to Phase 2)
- [x] Mock implementations for previews/tests: `MockLocationService`, `MockDestinationSearchService`
- [x] `Info.plist`: `NSLocationWhenInUseUsageDescription` added
- [ ] WeatherKit entitlement enabled and configured in the project (deferred — comes with the Phase 3 rain layer)
- [ ] `AGENTS.md` conventions verify: no comments, modern SwiftUI (`@Observable`), max 4-line rule for VMs
- [ ] Build passes, `@review` clean

## Phase 2 — Foundation Models & Routing MVP

Goal: user can search a destination and see route alternatives on a map (like Apple Maps).

- [ ] `Trip`, `RoutePlan`, `SavedDestination` models replace `Item`; registered in `Schema`
- [ ] Destination search (map-based + `MKLocalSearchCompleter` suggestions)
- [ ] `MKDirections` returns 2–3 route alternatives in `RoutePlan`
- [ ] Map with route polylines, start/destination pins, best-fit camera
- [ ] Route cards: ETA + distance (glanceable, ≥ 44 pt targets)
- [ ] `@MainActor @Observable` `TripPlannerViewModel` with `idle/loading/loaded/failed` state
- [ ] Build passes, `@review` clean

## Phase 3 — Rain Forecast Layer (feat/weather-forecast)

Goal: per-point rainfall forecast along the **selected** route so riders see where the road will be rained on, color-coded on the map with a legend + wet distance.

**Scope banner:** selected-route-only forecast (unselected alternatives get no weather; re-select re-fetches, cached routes reuse segments); no dry-route ranking / departure-time sweep (moved to Phase 4); no SwiftData/persistence; no changes to the trip-planner routing shell beyond wiring weather in; WeatherKit entitlement added in repo but **live WeatherKit = device-only manual validation** (mock-verifiable path now).

- [ ] WeatherKit entitlement `com.apple.developer.weatherkit` in `RainDodger/RainDodger.entitlements`
- [ ] Protocol-first `WeatherService` (Live via WeatherKit `currentWeather` + `minuteForecast`/`hourlyForecast`; Mock with deterministic scripted probabilities)
- [ ] Selected route polyline sampled every 5 km (cap ~60 samples); per-sample arrival time = `departureDate ?? now` + proportional fraction of travel time; `rainChance` fetched in a bounded TaskGroup (chunked ≤ 8)
- [ ] Blocking "Checking rain along your route…" loading overlay over the map while weather computes
- [ ] Color-coded route segments on the map (<30% route-blue / 30–60% yellow / ≥60% red) REPLACING the selected route's blue stroke; unselected alternatives muted as today
- [ ] `RainLegend` (Dry/Light/Heavy rain chips + % ranges) + wet-distance line ("14 km of 40 km with rain ≥ 50%"); text not color-only
- [ ] Offline/WeatherKit-fail degrades to a non-blocking "live rain unavailable" banner; route fully shown, plan not failed
- [ ] `@MainActor @Observable` `TripPlannerViewModel` injects `WeatherService`; `ContentView` injects `LiveWeatherService`; all previews use `MockWeatherService`
- [ ] Accessibility per 004 (VO labels, non-color-only, ≥ 44 pt, Dynamic Type, Reduce Motion)
- [ ] Build passes (`.opencode/scripts/xcode-tools.sh build`), `@review` clean

## Phase 4 — Departure Optimization & Saved Destinations (P1)

Goal: "leave in 20 min, rain passes in 40 min."

- [ ] `SavedDestination` flows: save, list, reuse (home/work)
- [ ] Departure-time sweep over hourly forecast → driest departure window suggestion
- [ ] Route cards update when departure time changes (re-score)
- [ ] Build passes, `@review` clean

## Phase 5 — Working App Polish (P2/P3)

Goal: rider-ready app, not a demo.

- [ ] Turn-by-turn with rain warnings ahead ("rain in 5 km")
- [ ] Core ML short-term rain predictor blended with WeatherKit per segment
- [ ] Design tokens from the feature design docs in `docs/designs/` applied (light + dark, typography, colors, monospaced rain %)
- [ ] Haptics on ride start + rain warning
- [ ] Glove-friendly audit: all targets ≥ 44 pt, high contrast, glanceable while mounted
- [ ] Landscape (mounted) layout supported
- [ ] Build passes, `@review` clean

## Phase R1 — Route Detail (feat/route-detail, docs)

Goal: a pre-ride route detail sheet for the selected route — tapping the already-selected route card expands the same planner sheet (`.medium` → `.large`) to a turn-by-turn step list (turn icons + instructions + distances) with rain marks on wet steps (representative `rainChance ≥ 50%`, max sample per step) and a header wet-distance summary shared with the map legend. Preview only — no live navigation, background location, or voice.

**Scope banner (R1 = docs phase only, no app code):** step-tap map highlight deferred; heading-delta turn inference deferred; weather stays explicit-check ("Check route for rain" → existing `checkRoute()`, no silent auto-fetch); English-only instruction parser (other locales degrade to `.other`); no SwiftData/persistence; reuses the existing 5 km `rainSegments` and ≥ 50% wet concept via a shared `RainMetrics.wetDistance(for:)` helper; stitched stop routes concatenate both legs' steps with continuous cumulative distances.

- [x] `docs/specs/route-detail.md` (Goal, User Problem, Requirements R1–R13, Data Model with `RouteStep`/`RouteTurnType` + mermaid ER, Rules/Logic incl. parser ordering + distance alignment + rain-per-step mapping + shared wet-distance, Constraints, Acceptance)
- [x] `docs/designs/route-detail.md` (screenshot-first: states detail-loaded/checking/no-forecast/unavailable/empty-steps, layout, `RouteDetailSheet`/`RouteStepRow` components, light/dark, exact VoiceOver labels, motion/Reduce Motion, ≥ 44 pt)
- [x] `docs/flows/route-detail.md` (related docs, flow goal, spec/design coverage, main journey, edge cases A–O, mermaid flowchart)
- [x] `docs/template/spec-guide.md` §9 updated to `feat/route-detail` (Models/Services/ViewModels/Views of this branch)
- [x] `docs/implementation.md` Phase R1 entry + Change Log row
- [x] `RouteStep`/`RouteTurnType` models + English instruction parser in `RainDodger/Models/RouteStep.swift`
- [x] `DirectionsService` (Live + Mock) builds distance-aligned `steps` from `MKRoute.steps`; stop routes stitch both legs' steps
- [x] `TripPlannerViewModel` maps `rainSegments` → per-step representative chance; wet-distance summary via shared `RainMetrics`
- [x] `RouteDetailSheet` + `RouteStepRow`; re-tap selected card expands the same sheet `.medium` → `.large`; back control preserves state
- [x] Accessibility per 004 (≥ 44 pt, exact VO labels, Dynamic Type, Reduce Motion, non-color-only); mocks in previews
- [x] Build passes (`.opencode/scripts/xcode-tools.sh build`), `@review` clean

## Phase R2–R6 — Route Detail (feat/route-detail, code)

Goal: implement the Phase R1 route-detail docs — re-tapping the **already-selected** route card expands the SAME planner sheet (`.medium` → `.large`) into a pre-ride turn-by-turn step list (turn icons + instructions + distances) with rain marks on wet steps (representative `rainChance ≥ 50%`, max sample per step) and a wet-distance header shared with the map legend. Preview only — no live navigation, background location, or voice.

**Scope banner (R2–R6 = code + final docs sync):** step-tap map highlight deferred; heading-delta turn inference deferred; weather stays explicit-check ("Check route for rain" → existing `checkRoute()`, no silent auto-fetch); English-only instruction parser (other locales degrade to `.other`); no SwiftData/persistence; reuses the existing 5 km `rainSegments` and ≥ 50% wet concept via the shared `RainMetrics` helper; stitched stop routes concatenate both legs' steps with continuous cumulative distances.

- [x] `RouteStep`/`RouteTurnType` models + ordered English instruction parser in `RainDodger/Models/RouteStep.swift` (13 cases; index-0 `.other` → `.straight` override per spec §5)
- [x] `DirectionsService` (Live + Mock) builds distance-aligned `steps` from `MKRoute.steps` (`scale = totalDistance / max(Σd, 1)`); stop routes stitch both legs with index/distance offsets; Mock scripts 8 deterministic steps
- [x] `TripPlannerViewModel` maps `rainSegments` → per-step representative chance via shared `RainMetrics.mappedSteps` (max-of-range + nearest-sample fallback); `loadWeather` rebuild preserves steps (hazard fix); `MapScreenView` legend refactored to `RainMetrics.wetDistance(for:)`
- [x] `RouteDetailSheet` + `RouteStepRow` (new); re-tap selected card expands the same sheet `.medium` → `.large` (detent selection owned by `TripPlannerSheet`, NOT a nested sheet); back control preserves origin/destination/stop/selection/departure
- [x] `MapScreenView`: `icloud.slash` fix for the pre-existing `cloud.slash` blank-symbol bug in `RainUnavailableBanner`; call-site detent removal
- [x] Accessibility per 004 (≥ 44 pt, exact VO labels, Dynamic Type, Reduce Motion, non-color-only); mocks in previews
- [x] Docs sync (R6): this Phase R2–R6 entry + Change Log row; `docs/template/spec-guide.md` §9 confirmed current; spec/design/flow sanity-checked vs shipped code
- [x] Build passes (`.opencode/scripts/xcode-tools.sh build`), `@review` clean

## Phase U1 — UI Enhancements & Fixes (fix/ui-enhancements)

Goal: resolve the 7 reported map-screen UI issues — recenter never hidden behind the bottom bar, search field pinned with no recents, Apple Maps-style long route-summary pill with a grabber (tap or drag-up reopens), editable destination row, forecast loading only on Check Route (no auto-fetch on re-selection) with the sheet dismissed and the overlay in front, and a clear "Route details" entry to the step view.

**Scope banner (U1 = docs + code):** weather fetched ONLY by `checkRoute()` (re-select reuses cached segments, never fetches); Check Route / "Check route for rain" dismiss the sheet first, then run `checkRoute()` (routing failure re-presents the sheet so Retry stays reachable); destination row editable via destination-mode `SearchPage` (pick saves recents); "Route details" row below the route cards is the primary step-view entry (re-tap of the selected card stays a shortcut); no new files (pbxproj untouched); no SwiftData/persistence; compass stays out of scope (commented out).

- [x] `docs/designs/maps-screen.md` — recenter bottom-trailing drawn above the bottom bar (~80 pt portrait / ~76 pt landscape), compass explicitly out of scope; denied-overlay copy reworded
- [x] `docs/specs/trip-planner.md` — R3 editable destination (re-plans, saves recents); R13 long pill + grabber, tap AND drag-up reopen; R16 dismiss-then-check + failure re-present; §5/§7 synced
- [x] `docs/designs/trip-planner.md` — destination row tappable (hint "Double tap to change destination"); long pill layout (minHeight ~56, grabber 36×5 `systemGray4`/`systemGray3` decorative a11y-hidden; full-width in landscape like the search capsule); Check Route dismisses sheet; "Route details" row (≥44 pt, chevron, VO "Route details" + hint); pill VO hint "Double tap or drag up to reopen trip planner"; §6 drag-up ≥40 pt + Reduce Motion
- [x] `docs/specs/weather-forecast.md` — R7 re-select NEVER fetches (cached `.loaded` reuse / `.idle`); §5 Trigger = `checkRoute()` only + in-flight task attaches only to its requested route; R3 sheet-dismissed + overlay covers map/pill
- [x] `docs/designs/weather-forecast.md` — §1 Loading "Check Route dismisses the sheet; overlay covers map + pill"; §1 re-selected route = cached reuse only
- [x] `docs/specs/route-detail.md` — R1 "Route details" row primary entry (re-tap shortcut); R6 "Check route for rain" dismisses then `checkRoute()`
- [x] `docs/designs/route-detail.md` — §1/§3 Details-row entry + dismiss-on-check noted
- [x] `docs/flows/trip-planner.md`, `docs/flows/weather-forecast.md`, `docs/flows/route-detail.md` — journeys, coverage tables, edges (J/K/N, A/B/E, A/C), mermaid nodes updated
- [x] `docs/template/spec-guide.md` §9 — branch identity → `fix/ui-enhancements` (file list unchanged — no new Swift files)
- [x] `TripPlannerViewModel` — `selectRoute` never fetches; `weatherState = selected.rainSegments.isEmpty ? .idle : .loaded`
- [x] `MapScreenView` — bottom bar overlay declared before the recenter stack (recenter draws on top, padded 80/76 pt); failure re-presents the trip sheet
- [x] `SearchPage` — root VStack pinned to the sheet top (empty-idle no longer vertically centered)
- [x] `RouteSummaryPill` — long pill + grabber + tap/drag-up (≥40 pt) reopen; VO hint updated
- [x] `TripPlannerSheet` — destination row editable (destination search mode, re-plans); Check Route dismiss-then-check; "Route details" row below the cards
- [x] `RouteDetailSheet` — "Check route for rain" dismiss-then-check
- [x] `LocationPermissionOverlay` — copy reworded ("…to recenter the map")
- [x] Accessibility per 004 (≥44 pt, exact VO labels/hints, Dynamic Type, Reduce Motion); no comments; mocks in previews
- [x] Build passes (`.opencode/scripts/xcode-tools.sh build`), `@review` clean (design-review PASS Phase 1, swift-review PASS Phase 2)

## Phase RT1 — Rain-Time Markers (feat/rain-time-markers, docs)

Goal: answer WHEN the rain hits, not just where — a small `RainTimeBadge` at the start of each wet route stretch showing the rider's estimated ARRIVAL time there (departure + proportional fraction of travel time — the exact time the forecast was sampled for), capped at 3 badges by route order, with the legend counting the rest.

**Scope banner (RT1 = docs phase only, no app code):** badge = `cloud.rain.fill` + locale-aware short time (e.g. "2:40 PM"; 24h locales "14:40"), NO "Rain" word on the badge; cap 3 (first 3 wet stretches by route order, most imminent); wet stretch = contiguous samples with `rainChance ≥ RainMetrics.wetThreshold` (0.5), badge at the stretch-start coordinate offset BELOW the route line (`y + 20`, clear of the destination ETA badge `y − 42` and fastest badge `y − 26`); badge recipe identical to `destinationETABadge` (Capsule, `Color(.systemBackground)`, `Color.primary`, shadow `black 0.25 / r4 / y2`, `.headline`, `.accessibilityHidden(true)`); departure change keeps existing behavior (`setDepartureDate` → `plan()` → `weatherState = .idle` clears badges + legend time until the next Check Route — no re-scoring without re-fetch; full departure-time re-scoring is Phase 4); overlap accepted (no distance filter, bounded by the cap); badges a11y-hidden — the extended `RainLegend` VO label (capped times + remainder count) is the carrier; NO time info in `RouteDetailSheet` this branch (deferred); no new Swift files (modified `RoutePlan.swift`, `TripPlannerViewModel.swift`, `MapScreenView.swift`, `RainLegend.swift`; pbxproj untouched).

- [x] `docs/specs/weather-forecast.md` — §1 where+when goal; new R10 (P0); §4 `RainSegment.arrivalDate` + `WetStretch` + mermaid; §5 stretch grouping / badge placement / cap / formatting / departure-change policy; §7 acceptance bullets
- [x] `docs/designs/weather-forecast.md` — §1 loaded gains badges + legend time line (+ departure-change clearing, cached instant); §2 badge layout (`y + 20`, cap 3, single line) + legend time line ("·" joined, "+N more"); §3 `RainTimeBadge` / `WetStretch` / `RainMetrics.wetStretches(for:)` / legend time input; §4 tokens (no raw hex); §5 exact extended VO label + hidden badges; §6 instant appear/disappear, no haptics
- [x] `docs/flows/weather-forecast.md` — journey steps 4/7/9, R10 coverage rows, edge cases J–M, mermaid stretch-grouping/badge nodes
- [x] `docs/template/spec-guide.md` §9 → `feat/rain-time-markers` (no new Swift files — modified `RoutePlan.swift`, `TripPlannerViewModel.swift`, `MapScreenView.swift`, `RainLegend.swift`)
- [x] `docs/implementation.md` Phase RT1 entry + this Change Log row
- Build skipped (docs only — verified via `git status --short` / `git diff --stat`)

## Phase RT2 — Rain-Time Markers (feat/rain-time-markers, code)

Goal: implement the Phase RT1 rain-time-marker docs — a small map badge at each wet-stretch start showing the rider's estimated arrival time (departure + proportional travel time, the exact time the forecast was sampled for), a legend time line, and the extended legend VoiceOver label.

**Scope banner (RT2 = code + final docs sync):** badge = `cloud.rain.fill` + locale-aware short time (no "Rain" word), cap 3 by route order (shared `RainMetrics.maxWetStretchBadges`), wet stretch = contiguous samples ≥ `RainMetrics.wetThreshold` (0.5) via `RainMetrics.wetStretches(for:)`, badge positioned below the route line (`y + 20`, clear of ETA `y − 42` / fastest `y − 26`); departure/route change clears badges + time line via the existing `weatherState = .idle` reset — the next Check Route recomputes (no re-scoring without re-fetch; Phase 4 = full departure-time re-score); cached re-select renders instantly; overlap between nearby badges accepted; badges `.accessibilityHidden(true)` with the extended `RainLegend` VO label as the carrier; no time info in `RouteDetailSheet` this branch; no new files (pbxproj untouched).

- [x] `RainDodger/Models/RoutePlan.swift` — `RainSegment.arrivalDate: Date` (required, no default); `WetStretch` (`id: Int` = index of the run's first wet sample — stable identity across renders and re-fetches); `RainMetrics.wetStretches(for:)` groups contiguous ≥ 0.5 runs (first segment's coordinate/arrival/distance, run max chance); `RainMetrics.maxWetStretchBadges = 3` shared cap
- [x] `RainDodger/ViewModels/TripPlannerViewModel.swift` — `sampleRain` passes the already-computed `arrival` into `RainSegment(...)`; sampling cadence/triggers unchanged (weather only via `checkRoute()`)
- [x] `RainDodger/Views/MapScreenView.swift` — `wetStretches` computed ONCE per render and threaded into `map(stretches:)`/`weatherStatusOverlay(stretches:)`/`mapBadges(proxy:stretches:)`; ≤ 3 `RainTimeBadge`s at stretch starts via `proxy.convert`, `y + 20`, a11y-hidden, recipe identical to `destinationETABadge` (Capsule, systemBackground, primary, shadow black 0.25/r4/y2, `.headline`), `cloud.rain.fill` + `Date.FormatStyle(date: .omitted, time: .shortened)`, `lineLimit(1)`
- [x] `RainDodger/Views/RainLegend.swift` — time line "Rain at … · …" (+" +N more"), separators/"+N more" `Color.secondary` via `AttributedString` (avoids deprecated `Text + Text` on iOS 26); VO label extended with the exact locked wording ("Rain likely at 2:40 PM, 3:05 PM, and 3:30 PM." / "N more wet stretches."); previews with 5 mock wet stretches + empty state
- [x] Docs sync: `docs/specs/weather-forecast.md` §4 `WetStretch.id` = Int (start-sample index); spec/design/flow sanity-checked vs shipped code
- [x] Accessibility per 004 (Dynamic Type, ≥ 4.5:1 both modes, non-color-only — time carried by legend text + VO, Reduce Motion — no animations added); no comments; mocks in previews
- [x] Build passes (`.opencode/scripts/xcode-tools.sh build`, EXIT 0), `@review` clean (design-review PASS Phase RT1, swift-review PASS Phase RT2)

## Release Gate (working app)

- [ ] All P0–P1 features verified on simulator + device
- [ ] Graceful offline behavior verified
- [ ] Backwards-compatible state migration (SwiftData schema changes safe)
- [ ] No API keys / `DEVELOPMENT_TEAM` / `xcuserdata/` in commits — checked at `@push`

## Change Log

Document every change made in each phase here — files added/modified, decisions, and anything that deviated from the plan.

| Date | Phase | Change |
|---|---|---|
| 2026-09-10 | Phase RT2 — feat/rain-time-markers (code + docs sync) | Implemented the RT1 docs: `RainSegment.arrivalDate` (required, exact sampled arrival) in `RoutePlan.swift`; `WetStretch` with `id: Int` = first wet sample's index (stable identity, no UUID churn per render); `RainMetrics.wetStretches(for:)` (contiguous runs ≥ `wetThreshold`, first-segment coordinate/arrival/distance + run max chance) + shared `maxWetStretchBadges = 3`. `TripPlannerViewModel.sampleRain` passes the pre-computed `arrival` into the segment (sampling/trigger cadence untouched). `MapScreenView` computes stretches ONCE per render and threads them into `map`/`weatherStatusOverlay`/`mapBadges`; renders ≤ 3 `RainTimeBadge`s (`cloud.rain.fill` + locale short time, `y + 20` below the line, a11y-hidden, recipe identical to `destinationETABadge`). `RainLegend` gains the time line ("Rain at … · …" + " +N more", secondary separators via `AttributedString` — `Text + Text` is deprecated on iOS 26) and the extended VO label with the exact locked wording. Deviations: Reviewer suggestions applied (shared cap constant, required `arrivalDate`, stable stretch id, compute-once); spec §4 `WetStretch.id` synced to Int; pre-existing Swift 5-mode actor-isolation warning at `TripPlannerViewModel` (MainActor-isolated `RainSegment` init in a task-group closure) left unfixed — track for a Swift 6-mode cleanup; badge placement/VO need a manual simulator/device look. |
| 2026-09-10 | Phase U1 — fix/ui-enhancements (docs + code) | Fixed the 7 reported map-screen UI issues, docs-first. **Docs (Phase 1, design-review PASS):** maps-screen design — recenter bottom-trailing drawn ABOVE the bottom bar (~80 pt portrait / ~76 pt landscape), compass explicitly out of scope, denied-overlay copy reworded ("recenter the map"); trip-planner spec/design — R3 destination row now EDITABLE (destination-mode SearchPage → `updateDestination` re-plans, pick saves recents; hint "Double tap to change destination"), R13 long pill + grabber (~36×5 pt, `systemGray4`/`systemGray3`, decorative a11y-hidden; width = full − 32 pt portrait / full-width landscape like the search capsule; minHeight ~56; tap AND drag-up ≥40 pt reopen; VO hint "Double tap or drag up to reopen trip planner"), R16 + Check Route dismiss-then-check with failure re-present, "Route details" row below the cards (≥44 pt, chevron, VO "Route details" + hint "Double tap to open step-by-step directions"); weather-forecast spec/design — R7 re-select NEVER fetches (cached `.loaded` reuse / `.idle`), Trigger = `checkRoute()` only, in-flight task attaches only to its requested route, Loading state = sheet dismissed + overlay covers map/pill; route-detail spec/design — "Route details" row primary entry (re-tap shortcut), "Check route for rain" dismiss-then-check; flows (trip-planner J/K/N, weather A/B/E, route-detail A/C) + mermaid synced; spec-guide §9 → fix/ui-enhancements. **Code (Phase 2, swift-review PASS, build succeeded):** `TripPlannerViewModel.selectRoute` — removed auto-fetch, `weatherState = selected.rainSegments.isEmpty ? .idle : .loaded`; `MapScreenView` — bottom-bar overlay declared before the bottomTrailing recenter stack (recenter on top, `.padding(.bottom, isLandscape ? 76 : 80)`), `.onChange(of: tripPlanner.state)` re-presents the trip sheet on `.failed` while dismissed; `SearchPage` — root VStack `.frame(maxWidth: .infinity, maxHeight: .infinity, alignment: .top)` (field pinned with no recents); `RouteSummaryPill` — long pill + grabber + `.simultaneousGesture(DragGesture(minimumDistance: 10))` reopening on upward translation < −40, tap kept; `TripPlannerSheet` — `SearchTarget` gains `.destination`, destination row is a Button → destination search → `updateDestination`, Check Route `dismiss()` then `checkRoute()`, "Route details" row below cardsArea (tap → `detent = .large; showsDetail = true`); `RouteDetailSheet` — "Check route for rain" `dismiss()` then `checkRoute()`; `LocationPermissionOverlay` — copy "…to recenter the map". Reviewer suggestions applied: maps-screen landscape bullet + denied copy dropped compass references; route-detail intro/§3 name the Details row as primary entry; weather spec §5 states the in-flight task's fate (backs flow edge B). Deviations: pill renders full-width in landscape (inherits call-site padding, matches the search capsule's landscape treatment; trip-planner design §2 landscape bullet synced accordingly); pre-existing `RainSegment` actor-isolation warning untouched. |
| 2026-09-10 | Phase R2–R6 — feat/route-detail (code + docs sync) | Code for the Phase R1 docs. **R2 — models + service:** added `RainDodger/Models/RouteStep.swift` (`RouteTurnType` with the 13 cases `depart`/`straight`/`turnLeft`/`turnRight`/`slightLeft`/`slightRight`/`keepLeft`/`keepRight`/`merge`/`roundabout`/`uTurn`/`arrive`/`other` + deterministic ordered English instruction parser — empty → `.other`, "arrive"/"destination" → `.arrive`, "depart"/"head " prefix → `.depart`, "roundabout"/"exit" → `.roundabout`, "u-turn"/"u turn"/"uturn" → `.uTurn`, "merge" → `.merge`, "keep left"/"keep right" → `.keepLeft`/`.keepRight`, "slight"/"bear left/right" → `.slightLeft`/`.slightRight`, "turn left"/"left turn"/"turn right"/"right turn" → `.turnLeft`/`.turnRight`, "continue"/"straight" → `.straight`, bare "left"/"right" fallback, else `.other`; **index-0 `.other` → `.straight` override** documented in spec §5; `RouteStep` struct with `id`/`index`/`instruction`/`distance`/`turnType`/`polyline`/`coordinatePoints`/`distanceFromStart` + `rainChance`/`wet`), modified `RainDodger/Models/RoutePlan.swift` (`RouteAlternative` gains `steps: [RouteStep] = []`), modified `RainDodger/Services/DirectionsService.swift` (Live `steps(from:totalDistance:)` maps `MKRoute.steps` with distance alignment `scale = totalDistance / max(Σd, 1)` so the last step ends exactly at the route distance; stop routes stitch both legs' steps via `indexOffset`/`distanceOffset`; Mock scripts 8 deterministic steps). **R3 — shared rain metrics:** `RainMetrics` helper in `RoutePlan.swift` — `wetThreshold` 0.5, `wetDistance(for:)` (single shared source of truth for the map legend AND the detail header), `mappedSteps(_:rainSegments:totalDistance:)` (representative chance = max `rainChance` of the 5 km samples inside the step's range, nearest-sample-by-midpoint fallback so no step is silently unmarked, empty segments → no marks); `TripPlannerViewModel.loadWeather` rebuild now preserves steps (**hazard fix** — the previous rebuild dropped them) and attaches per-step mapped chances; `MapScreenView` legend refactored to `RainMetrics.wetDistance(for:)` (private duplicate removed). **R4 — sheet UI:** added `RainDodger/Views/RouteDetailSheet.swift` (48×44 back chevron VO "Back to routes", "Route details" header, summary/checking/unavailable header states, step-list card with hairlines, empty state, full-width "Check route for rain" → `checkRoute()`) and `RainDodger/Views/RouteStepRow.swift` (turn icon + instruction + distance; wet steps add `cloud.rain.fill` + "%", VO "…, rain N percent"), modified `RainDodger/Views/RouteCard.swift` (selected chevron + VO hint "Double tap to open route details"), `RainDodger/Views/TripPlannerSheet.swift` (`@State detent` + `showsDetail`; `.presentationDetents([.medium, .large], selection: $detent)` ownership moved from the `MapScreenView` call site; re-tap of the selected card expands the SAME sheet — no nested sheet), `RainDodger/Views/MapScreenView.swift` (call-site detent modifier removed; **`cloud.slash` → `icloud.slash` deviation** — `cloud.slash` renders blank in SF Symbols, and the fix also repaired the pre-existing blank-symbol bug in the map's `RainUnavailableBanner`). Decisions: same-sheet detent expansion (Apple Maps style); pre-ride preview only (live navigation deferred); explicit-check weather policy preserved ("Check route for rain" button, expanding the sheet never fetches); icon + % marks on wet steps only (≥ 0.5); shared `RainMetrics` wet distance so legend and header can never disagree. Deviations: `cloud.slash` → `icloud.slash` (unavailable SF Symbol + pre-existing map banner bug fixed); `RouteStep` carries `index` (spec-mandated, spec §4). Docs: `docs/implementation.md` Phase R2–R6 entry + this Change Log row (Phase R1 docs entry's code checkboxes ticked); `docs/template/spec-guide.md` §9 confirmed current (`RouteStep.swift` new, `RouteDetailSheet.swift`/`RouteStepRow.swift` new, modified `RoutePlan.swift`/`DirectionsService.swift`/`TripPlannerViewModel.swift`/`RouteCard.swift`/`TripPlannerSheet.swift`/`MapScreenView.swift`); spec/design/flow sanity-checked against shipped code — no mismatches. Build passes (`.opencode/scripts/xcode-tools.sh build`). |
| 2026-09-10 | Phase R1 — feat/route-detail (docs) | Docs-only phase (no app code). Created `docs/specs/route-detail.md` (Goal, User Problem, Requirements R1–R13, Data Model with `RouteStep`/`RouteTurnType` + mermaid ER showing `ROUTE_ALTERNATIVE → ROUTE_STEP`, Rules/Logic with ordered English instruction parser, distance alignment, rain-per-step max-sample mapping + nearest fallback, shared `RainMetrics.wetDistance(for:)` for the "Rain on X km of Y km" header, sheet expansion/back rules, explicit-check weather policy, Constraints, Acceptance), `docs/designs/route-detail.md` (screenshot-first: states detail-loaded-with-wet-steps / checking-rain / no-forecast-with-button / weather-unavailable / empty-steps, layout, `RouteDetailSheet`/`RouteStepRow` components + `RouteTurnType` icon map, light/dark, exact VoiceOver labels e.g. "Turn left onto Main Street, 800 meters, rain 60 percent", motion/Reduce Motion), `docs/flows/route-detail.md` (related docs, flow goal, spec/design coverage, main journey Check Route → re-tap selected card → expand → steps + rain marks → back, edge cases A–O, mermaid flowchart). Updated `docs/template/spec-guide.md` §9 to `feat/route-detail` (Models `RainDodger/Models/RouteStep.swift` new + `RoutePlan.swift` modified; Services `DirectionsService.swift`; ViewModels `TripPlannerViewModel.swift`; Views `RouteDetailSheet.swift` · `RouteStepRow.swift` new + `RouteCard.swift`/`TripPlannerSheet.swift`/`MapScreenView.swift` modified). Added the Phase R1 entry + this Change Log row. Decisions: re-tap of the ALREADY-selected card expands the SAME planner sheet `.medium`→`.large` (no nested sheet) with a back control preserving origin/destination/stop/selection/departure; preview only (no live nav/background location/voice); step-tap map highlight deferred; English-only instruction parser; wet ≥ 50% (max representative sample per step); shared wet-distance helper so legend and detail header can never disagree; no silent auto-fetch ("Check route for rain" → existing `checkRoute()`); stitched stop routes concatenate both legs' steps with continuous cumulative distances; no SwiftData/persistence. |
| 2026-09-10 | Phase 3 — feat/weather-forecast | Added `docs/specs/weather-forecast.md`, `docs/designs/weather-forecast.md`, `docs/flows/weather-forecast.md` (screenshot-first: loading overlay, color-coded polyline, legend + wet distance, offline banner, a11y light+dark); updated `docs/template/spec-guide.md` §9 to `feat/weather-forecast`; rewrote the `docs/implementation.md` Phase 3 section to the sliced scope + scope banner. Code: added `com.apple.developer.weatherkit` entitlement (Boolean) to `RainDodger/RainDodger.entitlements`; added `RainDodger/Services/WeatherService.swift` (`WeatherService` protocol `rainChance(at:on:)`; `@MainActor LiveWeatherService` — `WeatherKit.WeatherService().weather(for:)`, arrival-time probability from `minuteForecast` (exact minute) → `hourlyForecast` (same hour) → `currentWeather.precipitationChance` fallback; `@MainActor MockWeatherService` — deterministic sin/cos of coordinate + arrival minute, hits all three bands); extended `RainDodger/Models/RoutePlan.swift` (`RainSegment` id/index/coordinate/distanceFromStart/rainChance + `rainSegments: [RainSegment] = []` on `RouteAlternative`); extended `RainDodger/ViewModels/TripPlannerViewModel.swift` (injects `weatherService`; `WeatherState` idle/loading/loaded/unavailable; `plan()` samples the selected route every 5000 m cap 60, per-sample arrival time = `departureDate ?? now` + fraction × travelTime, chunked ≤ 8 `withThrowingTaskGroup`, attaches segments to the selected alternative; `selectRoute` cancels prior weather task + re-fetches, reuses cached segments; weather failure → `weatherState = .unavailable`, plan kept); added `RainDodger/Views/WeatherLoadingOverlay.swift` (blocking scrim + spinner + "Checking rain along your route…", swallows map gestures, VO "Checking rain along your route") and `RainDodger/Views/RainLegend.swift` (`RainBand` dry/light/heavy with shared thresholds + colors, chips + % ranges, wet-distance line "X km of Y km with rain ≥ 50%", `ViewThatFits` fallback for Dynamic Type, single combined VO element); modified `RainDodger/Views/MapScreenView.swift` (weatherService injection; selected route drawn from cached per-segment `RainOverlay` polylines when segments exist else plain blue; loading overlay over the map; legend top-leading / offline banner in its place; wet distance computed from segments ≥ 0.5; overlays rebuilt on selection/weatherState change); modified `RainDodger/ContentView.swift` (`LiveWeatherService()` injection; preview mock); preview mocks in `TripPlannerSheet.swift`, `RouteSummaryPill.swift`, `DepartureTimePickerSheet.swift`. Decisions: **arrival-time probability derivation** — exact minute from `MinuteForecast` if arrival falls inside it, else same-hour `HourWeather`, else `CurrentWeather`; **bounded concurrency** via chunked TaskGroup (≤ 8 in flight) not a semaphore; **render caching** — `RainOverlay` polylines rebuilt on selection/weather-state change (not per frame) to avoid MapKit overlay churn during pan; **cached-route reuse** — re-selecting a route that already has segments reuses them (no wasted WeatherKit calls); live WeatherKit returns no data on the simulator → device-only manual validation. |
| 2026-09-02 | Phase 1 — feat/map-search | Added `RainDodger/Models/SearchResult.swift`; `RainDodger/Services/DestinationSearchService.swift` (protocol + `LiveDestinationSearchService` + `MockDestinationSearchService`), `RainDodger/Services/LocationService.swift` (protocol + `LiveLocationService` + `MockLocationService`); `RainDodger/ViewModels/MapSearchViewModel.swift`; `RainDodger/Views/MapSearchView.swift`, `RainDodger/Views/DestinationSearchField.swift`. Modified `RainDodger/ContentView.swift` (now hosts `MapSearchView` with Live services; template SwiftData list removed), `RainDodger/Info.plist` (`NSLocationWhenInUseUsageDescription` added), `docs/template/spec-guide.md` (§9 workspace table filled for the branch). Decisions: protocol-first services with Live + mock impls per MVVM; 350 ms debounce via `Task.sleep` + cancellation in the VM; custom floating search field over `.searchable` (full control of states, a11y, layout); map accessories via `.mapControls` (verified against the iOS 26.5 SDK — `mapAccessoryVisibility` does not exist); user-location dot via `UserAnnotation()` (position-based `Map` init has no `showsUserLocation` in iOS 26); pin-drop camera animation gated on Reduce Motion. Deferred: WeatherKit entitlement, routing (`MKDirections`), SwiftData `Item` replacement. |
| 2026-09-04 | Phase A — feat/maps-screen (docs) | **The 2026-09-02 "Phase 1 — feat/map-search" entry above is STALE on this branch.** None of its code exists here: `LocationService`/`DestinationSearchService`/`MapSearch*`/`SearchResult.swift` files and the `NSLocationWhenInUseUsageDescription` addition are absent (git history only; `RainDodger/` still contains the template), and the app is not reachable from a search field. `feat/maps-screen` **re-delivers** `LocationService` (protocol + Live + Mock) and the Info.plist key, renames the screen to `MapScreenView` + `MapViewModel`, and keeps `DestinationSearchService` **deferred** (search = tap stub, no `MKLocalSearch`). Docs created: `docs/specs/maps-screen.md` (Goal, User Problem, Requirements R1–R8, Data Model, Rules/Logic, Constraints, Acceptance), `docs/designs/maps-screen.md` (Docs-screenshot-first: screens/states, layout, components, light/dark, a11y with exact VoiceOver labels, motion/haptics; heading cone explicitly deferred), `docs/flows/maps-screen.md` (mermaid user flow + edge cases A–D). Updated `docs/template/spec-guide.md` §9 to `feat/maps-screen`; fixed stale `docs/specs/spec-guide.md` refs to `docs/template/spec-guide.md` in this file + `.opencode/rules/004-accessibility.md` (spec-guide moved in `a10dc33`, so the old path was a dangling pointer). Decisions: docs-only phase — no app code; user-location heading cone deferred; no routes/weather/follow-heading; no third-party packages; iOS 26 SwiftUI `Map` + `UserAnnotation`; Core Location heading with `trueHeading`/`magneticHeading` fallback; mock service scripts headings 0°→45°→90°→180° for previews. |
| 2026-09-04 | Phase B — feat/maps-screen (code) | Added `RainDodger/Services/LocationService.swift` (`LocationService` protocol; `LiveLocationService` `CLLocationManagerDelegate`, `.whenInUse`, `kCLLocationAccuracyBestForNavigation`, `headingFilter` 2°, `.portrait`, 10 s location timeout, `LocationError: LocalizedError`, heading stream `trueHeading ≥ 0` else `magneticHeading`; `MockLocationService` — configurable permission, Warsaw Żoliborz 52.2592/20.9916, scripted 0°→45°→90°→180° @1 s), `RainDodger/ViewModels/MapViewModel.swift` (`@MainActor @Observable`; `AuthorizationState` unknown/requesting/authorized/denied; `CameraIntent` recenter/resetNorthAndRecenter; 10 s timeout, heading outlier-drop ≥180° + exponential smoothing factor 0.2; `isCompassVisible` = |heading| > 5°; search/navigate stub messages), views `MapScreenView` (`Map(position:)` + `UserAnnotation()`, `.mapStyle(.standard)`, `.onChange` applies `CameraIntent`, `#Preview` mock), `DestinationSearchField` (56 pt capsule, magnifier + "Your Destination…" + mic decorative + avatar, VO "Your destination field"/"Double tap to search"), `CompassControl` (solid-backing needle, red north tip, rotation -heading eased; Reduce Motion static + read; VO "Compass"/"Double tap to reset to north and recenter"; hidden |heading| ≤ 5°), `MapControlButtonsView` (56 pt dark recenter `location.fill` + white navigate diamonds; VO "Recenter to my location", navigate "Start navigation"/"Double tap to begin"), `LocationPermissionOverlay` (denied: "Location Access Needed" + Open Settings 44 pt), `ComingSoonStub` (hammer glyph, VO "Dismiss"). Modified `RainDodger/ContentView.swift` (template SwiftData list → `MapScreenView` with Live services; import removed), `RainDodger/Info.plist` (`NSLocationWhenInUseUsageDescription` "Rain Dodger uses your location to center the map and show your position."). Decisions: **the iOS 26.5 SDK exposes `MapCameraPosition`/`UserAnnotation`/SwiftUI `Map` only via the cross-import overlay `@_MapKit_SwiftUI` (needs `import MapKit` + `import SwiftUI` in the same file; `import MapKit` alone fails — verified with `swiftc -typecheck`)** so the VM (never imports SwiftUI per rule 003) cannot hold a camera — VM publishes `CameraIntent`, the view owns `@State MapCameraPosition` and applies intents via `.onChange`; `nonisolated` delegate callbacks + `MainActor.assumeIsolated` due to `SWIFT_DEFAULT_ACTOR_ISOLATION = MainActor`/`SWIFT_APPROACHABLE_CONCURRENCY = YES`; PBXFileSystemSynchronizedRootGroup auto-picks up new files (no pbxproj edit); design §5 VO strings win over plan phrasings (search hint "Double tap to search", navigate "Double tap to begin"); Extra view files Overlay/Stub split per design §3 one-file-per-component. Verified: build clean (`xcode-tools.sh build`), sim screenshots loaded state (Żoliborz map + dot + capsule + button stack + compass hidden) and denied state (overlay + Open Settings). Sim gaps: compass needle motion/heading update is device-only (sim emits no heading); stub taps not UI-automated. Deferred: heading cone (user decision), `DestinationSearchService`/real search, routes/weather, `Item` removal. |
| 2026-09-05 | Phase C — maps-screen controls rework | User request (Google Maps–style reference): top-trailing stacked pair — compass on top, recenter below; navigate button dropped. `CompassControl` rebuilt: always-visible dark `#1C1C1E` circle (56 pt) with rotating cardinal letters N/E/S/W (N bold white, others 72% white) + fixed white top marker; dial rotates -heading (shortest-arc, 0.2 s eased; Reduce Motion static); VO "Compass" + value (cardinal + degrees) + hint reset-north. Removed `isCompassVisible`/hide-near-north from `MapViewModel` (always visible) and `navigateTapped` stub; deleted `MapControlButtonsView.swift` → `RecenterButton.swift` (white circle, dark `location.fill` arrow, VO "Recenter to my location"). `MapScreenView`: controls stacked `.topTrailing` (compass above recenter, 12 pt spacing, 16 pt insets), bottom-right overlay removed; error card no longer needs the button clearance inset. Docs synced: `docs/specs/maps-screen.md` (R6 navigate removed, R4 always-visible + cardinal letters), `docs/designs/maps-screen.md` (screenshot/layout/colors/a11y/motion), `docs/flows/maps-screen.md` (journey + mermaid, edge cases B/D), `docs/template/spec-guide.md` §9 Views list. Verified: `xcode-tools.sh build` + `run` on iPhone 17 sim — screenshot shows compass + recenter stack with system permission prompt; heading rotation is device-only (sim emits no heading). Deferred: no follow-heading mode, no navigate button this branch. |
| 2026-09-06 | Phase D — feat/search-feature (destination search) | Added `RainDodger/Models/SearchResult.swift` (`struct`, Identifiable/Codable/Hashable; `id` UUID, name, street, latitude, longitude, categorySymbol; computed `coordinate` from stored Doubles — `CLLocationCoordinate2D` is not Codable so never stored), `RainDodger/Models/RecentDestination.swift` (`@Model final class` with the same fields + `savedAt: Date`; `searchResult` convenience mapping). Added `RainDodger/Services/DestinationSearchService.swift` (`DestinationSearchService` protocol: `search(query:coordinate:)`, `recentDestinations()`, `saveRecent(_:)`; `@MainActor LiveDestinationSearchService` takes a SwiftData `ModelContext`, `MKLocalSearch` with `.pointOfInterest`/`.address` result types, region centered on coordinate (default span ~0.05), maps `MKPointOfInterestCategory` → SF Symbol (restaurant→fork.knife, cafe→cup.and.saucer, hotel→bed.double, gasStation→fuelpump, store/foodMarket→cart.fill, airport→airplane, publicTransport→bus.fill, park→tree.fill, default mappin), recents via `FetchDescriptor` sorted `savedAt` desc capped 10, save-on-select upserts by name + coordinates (fresh UUIDs per search make id-dedupe impossible) + drops oldest; `@MainActor MockDestinationSearchService` scripted Warsaw results + in-memory recents for previews). Added `RainDodger/ViewModels/SearchViewModel.swift` (`@MainActor @Observable`; `query`, `results`, `recents`, `state: SearchState` idle/searching/loaded/empty/error; 350 ms debounce via `Task.sleep` + cancellation; `onAppear` resets + loads recents; `queryChanged`/`clearQuery`/`select`; private `runSearch` guards cancellation). Added `RainDodger/Views/SearchPage.swift` (modal-sheet `SearchPage`: auto-focused field with magnifier + "Your Destination…" + decorative `mic.fill` `.accessibilityHidden(true)` + clear X (44 pt, VO "Clear search") only when `!query.isEmpty`; Recent section header shown only when recents non-empty; searching/loaded/empty/error states; row tap → `select` + `onSelect`) and `RainDodger/Views/SearchResultRow.swift` (circular systemGray5 icon + bold name + secondary street, ≥ 44 pt, `.accessibilityElement(children: .combine)` label "name, street" hint "Double tap to select as destination"). Modified `RainDodger/ViewModels/MapViewModel.swift` (added `selectedDestination`, `isSearchPresented`, `currentCoordinate`; `CameraIntent.focusDestination(CLLocationCoordinate2D)`; `searchFieldTapped()` → `isSearchPresented = true`; `selectDestination(_:)` sets destination + `focusDestination` + dismisses search; removed `showComingSoon`/`comingSoonMessage`/`dismissComingSoon`), `RainDodger/Views/MapScreenView.swift` (`let viewModel` → `@Bindable var viewModel`, added `searchService` + `@State searchViewModel` in `init`, `.sheet(isPresented: $viewModel.isSearchPresented)` → `SearchPage` with `searchViewModel.searchCoordinate = viewModel.currentCoordinate`, `.focusDestination` handled in `onChange(of: cameraIntent)` → `cameraPosition = .region(span ~0.01)`, destination `Marker`/annotation when `selectedDestination != nil`, removed `ComingSoonStub` overlay + animation, `#Preview` uses `MockDestinationSearchService`), `RainDodger/ContentView.swift` (creates `LiveDestinationSearchService` with `@Environment(\.modelContext)` and passes to `MapScreenView`). Deleted `RainDodger/Views/ComingSoonStub.swift`. Registered `RecentDestination.self` in the `Schema` in `RainDodger/RainDodgerApp.swift` (`Item.self` kept). Docs created: `docs/specs/search.md`, `docs/designs/search.md`, `docs/flows/search.md`; updated `docs/template/spec-guide.md` §9 to `feat/search-feature`; filled `.opencode/branch-goals/feat/search-feature.md`; updated `docs/specs/maps-screen.md` R5 + `docs/designs/maps-screen.md` + `docs/flows/maps-screen.md` search-stub refs to point at the new search docs. Sheet rework (post-build feedback): field pinned at sheet top, auto-focus applied with a 150 ms settle delay so the keyboard reliably appears, trailing circular close button (`@Environment(\.dismiss)`) distinct from the in-field clear X (`xmark.circle.fill`), `.presentationDragIndicator(.visible)`, dark-mode contrast — field/close use `systemGray5` in dark and result rows become `secondarySystemBackground` 16 pt cards. Light palette (user-specified): sheet background `Color.searchBackground` #F1F2F6, elements/cards `Color.searchElement` #C6C6C8; dark mode unchanged. Palette + typography extensions live in `RainDodger/Extensions/` (`ColorPalette.swift` Color extension: `searchBackground`/`searchElement` — app-wide palette home; `Typography.swift` Font extension: `rdSearchField`, `rdRowName`, `rdRowStreet`, `rdSectionHeader`), used by `SearchPage`/`SearchResultRow`. Decisions: SwiftData `@Model` for recents (not UserDefaults); SF Symbols mapped from `MKPointOfInterestCategory`; recents saved on select; `SearchResult` stores Doubles + computed `coordinate` (Codable-safe). Deferred: From/To/Next Stop fields, routing/MKDirections, weather, Trip/SavedDestination, template `Item` cleanup. |
| 2026-09-07 | Phase E — feat/destination (docs) | Docs-only phase (no app code). Created `docs/specs/trip-planner.md` (Goal, User Problem, Requirements R1–R12, Data Model with mermaid ER, Rules/Logic, Constraints, Acceptance), `docs/designs/trip-planner.md` (screenshot-first: states sheet-closed / auto-open-loading / loaded / failed / origin-unset / denied-location, half-modal layout, `TripPlannerSheet`/`RouteCard`/reused `SearchPage` components, light/dark, exact VoiceOver labels — route card "Route N, X minutes, Y km, selected/not selected", origin "Origin, Current location"/"Origin, Set origin", destination "Destination, <name>", "Add stop", "Remove stop", motion/haptics with Reduce Motion), `docs/flows/trip-planner.md` (related docs, flow goal, spec/design coverage tables, main journey pick → auto-open → origin=current → request → alternatives → select → camera fit → re-route, edge cases A–I, mermaid flowchart). Updated `docs/template/spec-guide.md` §9 to `feat/destination` (Models `RainDodger/Models/RoutePlan.swift`, Services `RainDodger/Services/DirectionsService.swift`, ViewModels `RainDodger/ViewModels/TripPlannerViewModel.swift`, Views `TripPlannerSheet.swift` · `RouteCard.swift` + modified `MapScreenView.swift`/`SearchPage.swift`, entry `ContentView.swift`; build target unchanged). **Branch slices the Phase 2 "Routing MVP" umbrella** — out of scope: weather/rain layer, `SavedDestination`/`Trip` SwiftData, route persistence, transport modes, multiple stops (max 1 stop). Locked decisions: **(1) routing** — no stop → one `MKDirections` request (source/destination, `requestsAlternateRoutes = true`, `transportType = .automobile`) returning 2–3 alternatives; stop present → two leg requests (origin→stop, stop→destination) stitched into ONE combined route (summed distance/ETA, polyline built from concatenated leg polylines), so with a stop the rider gets exactly 1 combined route; 0 routes returned = failed state; **(2) the sheet auto-opens even when location is denied/unknown** with origin optional (UNSET → "Set origin", no auto-route until an origin is picked). Data model is in-memory value types (`RouteWaypoint`, `RouteAlternative`, `RoutePlan`) — no SwiftData on this branch. Verified: `xcode-tools.sh build` passes unchanged. Deferred: routing code (Phase F), weather (Phase 3), departure optimization + saved destinations (Phase 4). |
| 2026-09-08 | Phase F — feat/destination (routing models + service) | Added `RainDodger/Models/RoutePlan.swift` (`RouteWaypoint` id/name/latitude/longitude with computed `coordinate` from stored Doubles — `CLLocationCoordinate2D` is not Codable so never stored; `RouteAlternative` id/distance/travelTime/polyline/coordinatePoints; `RoutePlan` origin?/destination/stop?/alternatives/selectedRouteID — all in-memory value types, no SwiftData on this branch). Added `RainDodger/Services/DirectionsService.swift` (`DirectionsService` protocol `route(from:to:via:maxAlternatives:)`; `@MainActor LiveDirectionsService`; `@MainActor MockDirectionsService` scripted alternatives/stitch for previews). Live impl: requests built from `MKMapItem(location:address:)` (replaces deprecated `MKMapItem(placemark:)`), `requestsAlternateRoutes = true`, `transportType = .automobile`; no stop → one request, alternatives kept up to `maxAlternatives` (3); stop → two leg requests (origin→stop, stop→destination) stitched into ONE combined route (summed ETA/distance, polyline built from concatenated leg coordinates); 0 routes → `DirectionsError.emptyResponse`. Modified `RainDodger.xcodeproj/project.pbxproj` (new files registered), `RainDodger/ContentView.swift` (injects `LiveDirectionsService` into `MapScreenView`). **SDK finding + decision deviation: `MKDirectionsRequest` has no stops property — single-request stop routing is unsupported, so the original single-request-with-stops assumption is replaced by the two-leg stitch** (spec R5 confirmed; `MKMapItem(location:address:)` needed for raw coordinates since waypoints have no placemarks). |
| 2026-09-08 | Phase G — feat/destination (TripPlannerViewModel) | Added `RainDodger/ViewModels/TripPlannerViewModel.swift` (`@MainActor @Observable`; `TripPlanState` idle/loading/loaded/failed(String); `state`, `routePlan`, `origin`/`destination`/`stop` waypoints, `selectedRouteID`; `updateDestination`/`updateOrigin`/`setOriginFromCurrentLocation`/`addStop`/`removeStop`/`selectRoute`/`plan`/`clear`). Decisions: `plan()` calls `cancelPlan()` FIRST — an origin/stop re-route cancels any in-flight `planTask` before validation, then guards (origin+destination present, origin ≠ destination, stop ≠ origin/destination) fail with graceful `.failed` messages instead of throwing; `applyRoute` rebuilds the immutable `RoutePlan`; `selectRoute` re-derives `routePlan` with the picked id; `maxAlternatives` 3; origin default "Current location". |
| 2026-09-08 | Phase H — feat/destination (Phase 4 UI) | Added `RainDodger/Views/TripPlannerSheet.swift` (half-modal `.sheet` over the map, `presentationDetents([.medium, .large])` + drag indicator, drag-only dismissal; origin row → destination row (read-only) → add-stop slot → cards area; idle/loading(`ProgressView`)/loaded(cards)/failed(message + Retry) states), `RainDodger/Views/RouteCard.swift` (ETA "X min" + "Y km", ≥ 44 pt, selected = blue stroke + checkmark, VO "Route N, X minutes, Y km, selected/not selected" + `.isSelected` trait), `RainDodger/Views/RouteSummaryPill.swift` (bottom-center capsule ≥ 44 pt, "X min · Y km" when loaded / "Calculating…" while loading, VO "Route summary, …" + hint, tap reopens sheet with state preserved). Modified `RainDodger/Views/MapScreenView.swift` (trip sheet auto-opens on destination pick; route polylines drawn — selected blue 6 pt / others 40% opacity 2 pt; origin marker hidden when it equals current location; camera fit on `routePlan.selectedRouteID` change, animates 0.45 s / instant under Reduce Motion), `RainDodger/ViewModels/MapViewModel.swift` (`selectDestination` sets `isTripSheetPresented = true`; `currentCoordinate` drives origin default + focus intent), `RainDodger/ContentView.swift` (#Preview mock wiring). Findings/fixes: `.failed` needs an associated-value pattern-match (`if case .failed = state`) — plain `==` against the enum case doesn't compile; `.accessibilityHidden` is unavailable on `MapPolyline` — polylines are pure graphics not exposed to VoiceOver, so route info lives in the cards + pill per rule 004. |
| 2026-09-08 | Phase I — feat/destination (SearchPage modes + origin/stop wiring) | Modified `RainDodger/Views/SearchPage.swift` (`SearchContext` destination/origin/stop: placeholders "Your Destination…"/"Where from?"/"Add a stop", mode-aware VO result hints, `savesRecents` only in destination mode — origin/stop picks never pollute destination recents; `context` param defaults to `.destination` so the destination path is unchanged), `RainDodger/Views/SearchResultRow.swift` (configurable `hint` for the VO hint per mode). Modified `RainDodger/Views/TripPlannerSheet.swift` (origin row + empty add-stop slot now buttons → nested `.sheet(item:)` presenting the reused `SearchPage` in origin/stop mode; pick calls `updateOrigin`/`addStop` → re-plan + camera refit via the existing `selectedRouteID` onChange; destination row stays read-only; filled stop keeps ≥ 44 pt X "Remove stop" → `removeStop`). Modified `RainDodger/Views/MapScreenView.swift` (pill gate now STATE-based: `state != .idle && !failed` — so "Calculating…" shows while a request is in flight during initial routing, per R13/edge J; `TripPlannerSheet` call site passes `searchService` + `searchCoordinate`), `RainDodger/ViewModels/TripPlannerViewModel.swift` (applyRoute selects the FIRST alternative on every new plan — a stale `selectedRouteID` from a previous plan's fresh UUIDs can never survive a re-route). |
| 2026-09-08 | Phase K — feat/destination (UI redesign code) | Implemented the Phase J docs redesign. **`RainDodger/Services/DirectionsService.swift`**: protocol + Live + Mock `route(...)` gained `departureDate: Date?`; `LiveDirectionsService` sets `request.departureDate` when non-nil (both stop legs + no-stop request, traffic-aware ETA); Mock accepts/ignores it (scripted results stay deterministic). **`RainDodger/ViewModels/TripPlannerViewModel.swift`**: `private(set) var departureDate: Date?`; `setDepartureDate(_:)` clamps past to now (`max(date, Date())`, flow edge L), nil = "Leave now", always re-plans; `plan()` passes the captured value; `clear()` resets it. **`RainDodger/Models/RoutePlan.swift`**: `RouteWaypoint` gained optional `categorySymbol: String?` (default nil) so the destination row can render the orange category icon from `SearchResult.categorySymbol` — small model addition the Phase J design required (spec §4's waypoint fields predate the redesign). **`RainDodger/Views/TripPlannerSheet.swift`** reworked to the design: bold centered "Direction" title; ONE grouped rounded card (`.secondarySystemBackground`/`.searchElement`) wrapping origin (blue `location.north.line.fill`, "Origin" secondary + "My Location"/"Set origin" primary, tap → origin search), destination (orange `categorySymbol` icon, read-only), add-stop (blue `plus`, "Add stop" → stop search; filled = "Stop" + name + 44 pt X "Remove stop"); each row carries a decorative `line.3.horizontal` handle `.accessibilityHidden(true)` placed OUTSIDE the tappable button so handle taps do nothing (edge P); "Leave at" row (secondary label + gray `systemGray6/5` pill "Now"/formatted time, ≥ 44 pt, VO "Leave at, Now"/"Leave at, <time>" + hint) presents the new picker sheet; full-width "Check Route" button `Color.checkRouteBlue` (~#0054D4) white text + `scooter` icon (verified available; fallback `figure.outdoor.cycle` documented), VO "Check Route" + hint, action `plan()` — **failed state now shows only the message; the Check Route button doubles as Retry (no separate Retry control)**; route cards + loading/idle states + nested search wiring kept; no close/X. **`RainDodger/Views/DepartureTimePickerSheet.swift` (new)**: Apple Maps–style medium sheet — "Leave at" header, `DatePicker` (`.wheel`, date + hourAndMinute, `in: Date()...` min = now), "Leave now" resets to nil, "Done" applies `setDepartureDate` + re-plan; VO "Choose departure date and time"/"Leave now"/"Done" with hints; swipe-dismiss without a pick does NOT re-plan (edge M). **`RainDodger/Views/MapScreenView.swift`**: map badges via `MapReader` + `proxy.convert(_:to: .local)` + `.overlay` — white ETA pill (`Color(.systemBackground)` + `Color.primary`, dark-adapted) at the destination and blue `checkRouteBlue` "X min Fastest" badge at the selected route's polyline midpoint, both `.accessibilityHidden(true)`, render even with one alternative (edge O), update on `selectedRouteID`; `selectDestination` passes `result.categorySymbol` into the destination waypoint. **`RainDodger/Extensions/ColorPalette.swift`**: new `Color.checkRouteBlue = Color(red: 0, green: 0.33, blue: 0.83)` (~#0054D4, white-on-blue ≈ 6.8:1) used by the button + Fastest badge. User decisions retained (not re-litigated): drag-only dismissal (no X), route cards retained + badges added, auto-plan on destination pick retained — Check Route is an idempotent `plan()` refresh doubling as Retry. Scope: `departureDate` lives only on the VM/request (not stored); weather re-scoring stays Phase 4. **The maps-screen top-trailing control stack (compass above recenter, 12 pt spacing, 16 pt trailing inset) was verified preserved during the redesign — restored after a transient removal when the badge overlays were added; `CompassControl` + `RecenterButton` remain in `MapScreenView` with their VO labels and 56 pt targets per `docs/designs/maps-screen.md`.** |
| 2026-09-08 | Phase L — feat/destination (departure picker rework) | **`RainDodger/Views/DepartureTimePickerSheet.swift`** reworked to the Apple Maps "Leave at" style. `.large` sheet + drag indicator. Header: gray circle X (≥ 44 pt, VO "Cancel") dismisses with no VM call · centered bold "Leave at" (`.isHeader`) · `Color.checkRouteBlue` filled circle with white `arrow.up` (≥ 44 pt, VO "Apply", hint "Apply departure time and re-route") → `setDepartureDate(draftDate)` + dismiss. Calendar: `.graphical` date-only `DatePicker` ("Choose departure date", `in: Date()...` min = today, past days disabled, `.labelsHidden()`, `.tint(Color.checkRouteBlue)`). Time row: secondary "Time" label + gray capsule pill (`systemGray6/5` backing, `Color.primary` short-time text) → nested `.sheet` `.height(300)`: bold "Time" title + `.wheel` `[.hourAndMinute]` picker bound to `timeDraft`; blue "Done" merges the wheel time into `draftDate` (day from draft, hour/minute from timeDraft) and dismisses; swipe-dismiss = draft unchanged (edge Q). "Leave Now" row: `Color.blue` text button (VO "Leave now", hint "Reset departure to now") → `setDepartureDate(nil)` + dismiss. **Draft-commit:** view-local `@State draftDate = viewModel.departureDate ?? Date()` (default = now's time; time component preserved on date change); re-plan happens ONLY on arrow-apply or Leave Now through the VM's single `setDepartureDate` commit point; X/swipe-dismiss makes no VM call (edge M); VM clamps a past applied draft to now (edge L). All targets ≥ 44 pt; sheet `Color(.systemBackground)`, X circle `systemGray5/4`, pill `systemGray6/5` per light/dark; system sheet transitions only (Reduce Motion); no comments; `#Preview` with `MockDirectionsService()`. Docs synced: `docs/designs/trip-planner.md` (§1 state bullet, §3 DepartureTimePickerSheet bullet, §4 light/dark, §5 VO, §6 motion + drag-only/44 pt clarifications), `docs/specs/trip-planner.md` (R15 rewritten, R9 notes → A–Q, §5 departure-time rule gains draft-commit, §7 acceptance updated), `docs/flows/trip-planner.md` (journey step 8 rewritten, edges L/M rewritten + NEW edge Q, R15 → Steps 8/14/edges L–M/Q + R9 → A–Q + design "Leave-at picker" → Steps 8–9/edge Q coverage rows, mermaid departure nodes updated and rendered valid via `render-diagrams.sh`). **`TripPlannerViewModel.swift`, `DirectionsService.swift`, `TripPlannerSheet.swift` untouched** (sha256-identical to the Phase K baseline — verified). SDK findings: `.graphical` DatePicker renders the month grid (weekday header, today highlight, filled-circle selection) and disables past days via `in: Date()...`; nested sheet-on-sheet (trip → picker `.large` → `.height(300)` time sheet) presents/dismisses as a normal SwiftUI stack. Time-of-day preservation across a day change relies on SwiftUI `.graphical` behavior and day-change interaction was not automatable in the launch smoke — flagged for a manual tap-through. Verified: `bash .opencode/scripts/xcode-tools.sh build` + `run` (smoke screenshot below). |
| 2026-09-10 | Phase 3 — feat/weather-forecast | Weather decoupled from auto-routing: `TripPlannerViewModel.plan()` became `private runRouting(loadWeather:)` with `weatherState = .idle` reset right after `cancelPlan()` (before guards) so ANY re-route clears a previous forecast/overlay; `plan()` = auto-routing only (destination/origin/stop/departure changes re-route with NO weather and no overlay); new `checkRoute()` runs routing + weather for the selected route. Check Route PRESERVES the previously selected alternative by INDEX across the fresh UUIDs DirectionsService returns (`applyRoute` gained `selectedIndex`, falling back to first only when no prior selection existed) so re-checking forecasts the rider's chosen route. `TripPlannerSheet` "Check Route" button now calls `viewModel.checkRoute` with a forecast accessibility hint; label unchanged. Docs updated: `docs/flows/weather-forecast.md` (flow goal, steps 1 + 9, edge E, mermaid nodes A + N), `docs/specs/weather-forecast.md` (§1 Goal, §5 Re-plan + Trigger bullets, §7 acceptance bullet). R7 (re-select re-fetch) wording unchanged. |
